from django.apps import AppConfig


class BaseConfig(AppConfig):
    name = 'apps.base'
#Home Navbar Footer Landing Páginas generales Templates compartidos
#se utilizará únicamente para funcionalidades generales del proyecto (como la página de inicio y vistas comunes)